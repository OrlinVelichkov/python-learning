cities = ["Sofia", "Varna", "Pl1Vdiv", "Burgas", "Pleven"]
print(cities[len(cities)//2][len(cities[len(cities)//2])//2])



# Да, за такъв вложен израз четенето "отвътре навън" е точно правилният подход — Python го оценява по същия начин.

# Ето реда на изчисление на cities[len(cities)//2][len(cities[len(cities)//2])//2]:

# len(cities) — дължината на списъка cities
# len(cities)//2 — средният индекс на cities (цялочислено деление)
# cities[len(cities)//2] — елементът на средна позиция (примерно средният "град" — низ или подсписък)
# len(cities[len(cities)//2]) — дължината на този среден елемент
# len(cities[len(cities)//2])//2 — средният индекс вътре в него
# cities[len(cities)//2][len(cities[len(cities)//2])//2] — крайният резултат: взима се символът/елементът на средна позиция от средния елемент на cities
# Тоест кодът намира средния елемент на средния елемент от cities (напр. средната буква от средното по позиция име на град).

# Важен детайл: изразът cities[len(cities)//2] се среща два пъти в реда — веднъж за да се изчисли неговата дължина (стъпка 4), и втори път за реалното индексиране (стъпка 6). Python го изчислява отделно и на двете места (не го кешира), така че реално се случва двойна работа.

# Именно затова по-четим начин би бил:


# middle_city = cities[len(cities) // 2]
# middle_char = middle_city[len(middle_city) // 2]
# print(middle_char)
# Това прави абсолютно същото, но избягва повторното изчисление и е много по-лесно за четене — не се налага да "разгъваш" вложените скоби наум.